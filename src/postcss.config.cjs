// eslint-disable-next-line no-undef
module.exports = {
  // eslint-disable-next-line no-undef
  plugins: [require('postcss-nested')]
  // postcss-nested 用于在 CSS 中实现嵌套规则，类似于 Sass 或 Less 中的嵌套语法。它允许开发者编写更加简洁和结构化的 CSS 代码。
}
